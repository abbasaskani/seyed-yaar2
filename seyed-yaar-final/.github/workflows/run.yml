name: run

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  run:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install -U copernicusmarine

      - name: Prepare latest dirs (always create non-hidden files so artifacts never fail)
        run: |
          rm -rf latest/runs/main || true
          rm -rf latest/verify || true
          rm -rf latest/logs || true
          mkdir -p latest/logs latest/verify
          echo "keep" > latest/logs/keep.txt
          echo "keep" > latest/verify/keep.txt

      - name: Run daily
        shell: bash
        run: |
          set -euo pipefail
          export PYTHONPATH="$GITHUB_WORKSPACE/backend"
          python -m seydyaar run-daily             --out latest             --past-days "${SEYDYAAR_PAST_DAYS:-1}"             --future-days "${SEYDYAAR_FUTURE_DAYS:-5}"             --step-hours "${SEYDYAAR_STEP_HOURS:-12}"             --grid "${SEYDYAAR_GRID:-160x160}"             --species "${SEYDYAAR_SPECIES:-skipjack}"

      - name: Upload logs + verify
        uses: actions/upload-artifact@v4
        with:
          name: seydyaar_verify_and_logs
          path: |
            latest/logs/**
            latest/verify/**
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 7

      - name: Commit latest
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add latest
          git commit -m "Update latest (auto)" || echo "No changes to commit"
          git push
